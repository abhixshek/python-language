#!/bin/bash
echo 'this is a bash script. will be executable on the terminal/gitbash using "./filename.sh" command'
echo 'very cool indeed'
echo 'this was all possible because of the shebang line (#!/bin/bash) at the top of the file that lets the shell know that this is a bash executable script.'

